/**
 * Este arquivo fornece declarações de tipos para o date-fns v4
 * Permite que o TypeScript encontre corretamente as funções exportadas
 */

// Não é necessário declarar explicitamente os tipos quando usando a versão 4+
// O pacote já fornece suas próprias definições de tipos

declare module 'date-fns' {
  // Declaração vazia para permitir importações
  // Isto permite que o TypeScript encontre o módulo sem erros
}
