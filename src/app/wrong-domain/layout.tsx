/**
 * Layout para página de domínio incorreto
 * Renderização limpa sem interferências
 */
export default function WrongDomainLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
